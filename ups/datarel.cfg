# -*- python -*-

import lsst.sconsUtils

dependencies = {
    # Dependencies only needed to build Doxygen correctly.
    "buildOptional": ["ctrl_events", "ap"]
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[], libs=[],
    hasDoxygenInclude=False,
    hasSwigFiles=False,
)
