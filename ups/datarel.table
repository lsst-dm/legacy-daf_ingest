setupRequired(meas_pipeline >= 4.3.1.0)
setupRequired(ip_pipeline >= 4.3.1.0)
setupRequired(ap >= 4.2.0.2)
setupRequired(ctrl_sched >= 4.3.0.0)
setupRequired(sdqa >= 4.1.0.0)
setupRequired(ctrl_orca >= 4.3.0.1)

# setupOptional(obs_cfht >= 4.1.0.0)
setupOptional(obs_lsstSim >= 4.1.0.1)
setupOptional(astrometry_net_data)
setupOptional(matplotlib)	# used by some debugging routines
setupOptional(cat >= 4.4.1.0)   # for database ingest
setupOptional(scisql >= 0.1)    # for database ingest

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envAppend(PATH, ${PRODUCT_DIR}/bin/sst)
envAppend(PYTHONPATH, ${PRODUCT_DIR}/bin/sst)
